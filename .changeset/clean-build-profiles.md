---
"eve": patch
---

`eve build --profile` now reports Rolldown invocation time and module traversal totals by authored-module and workflow build category, making repeated bundler work visible before build-pipeline optimization.
