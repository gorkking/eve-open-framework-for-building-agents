---
"eve": patch
---

Allow agents declared in an eve workspace to own a package.json when they are claimed by the root package-manager workspace. A member package's build script now builds its Vercel service, enabling agent-specific dependencies and build steps.
