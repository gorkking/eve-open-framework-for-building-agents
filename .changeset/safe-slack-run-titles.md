---
"eve": patch
---

Slack direct messages now use `Run` as their default Agent Runs title instead of exposing message content. Slack message hooks and generic event sends can set an explicit run title without changing the model input.
