---
"eve": minor
---

Add channel gates for HITL responses, session resumes, turn cancellation, session reset, and proactive channel receives. Public follow-up, cancel, reset, and `Session.cancel` operations now require actor auth or explicit `null`.
