---
"eve": patch
---

Add path-authored memory slots with configurable namespaces, required trusted scopes, a `byPrincipal` resolver from `eve/memory/scope`, provider `recall`, `save`, and `tools` lifecycle methods, scope-aware projections, and configurable cross-scope visibility.
