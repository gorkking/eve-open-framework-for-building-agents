---
"eve": patch
---

Add path-authored memory slots with configurable namespaces, required trusted scopes, a `byPrincipal` resolver from `eve/memory/scope`, scope-aware projections, configurable cross-scope visibility, and async turn-scoped provider tools backed by dynamic tool replay.
