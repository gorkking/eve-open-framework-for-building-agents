---
"eve": patch
---

Add first-class, path-authored memory slots with awaitable trusted scope locking, durable provider-owned memory messages, lifecycle hooks, and memory-scoped tools built on `defineDynamic`.
