---
"eve": patch
---

Add path-authored memory slots with required trusted scopes, provider `recall`, `save`, and `tools` lifecycle methods, scope-aware projections, and configurable cross-scope visibility.
